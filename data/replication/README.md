# Replication bundle

This small bundle lets a clone display and redraw the reported results without raw accident deliveries, daily counter records, or the 10-minute weather archive. `working/` contains the three small inspection tables; `tables/` contains exact numerical result tables; and `figures/` is the reference output snapshot. Run `python -m src.reproduce_results` to draw the core result figures again from the result tables.
